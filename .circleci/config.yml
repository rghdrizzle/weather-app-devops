
version: 2.1
orbs:
  node: circleci/node@4.7
jobs:
  build-and-test:
    executor: 
      name: node/default
    steps:
      - run: npm install
      - run: echo "done"

workflows:
  build-and-test:
    jobs:
      - node/test:
          version: '16.10'
          pkg-manager: npm
      - build-and-test:
          filters:
            branches:
              only: master


